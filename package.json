{
  "name": "sunposition",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8080",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^3.0.0"
  },
  "dependencies": {
    "gsap": "^3.10.4",
    "suncalc": "^1.9.0",
    "three": "^0.143.0"
  }
}
